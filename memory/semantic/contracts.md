# Contracts

The FastAPI OpenAPI schema is the API source of truth. The MVP exposes checkpoint, prediction, repair, and evidence endpoints. Nicknames stay in browser session storage and must never appear in a request contract.


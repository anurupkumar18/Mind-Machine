# Public-data boundary

Use public or synthetic prompts and code by default. A learner may explicitly authorize a local workspace for a session only when its contents remain local and unpersisted; exclude credentials, private keys, and detected sensitive content from the source map. Do not retain learner responses on the backend or add repository upload, Canvas/GitHub OAuth, credentials, or student records without separately implemented security controls.
